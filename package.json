{
  "name": "ferromaderas-inventory-sync",
  "version": "1.0.0",
  "description": "Sincroniza inventario Excel (Dichara) con la API de Ferromaderas. Ejecutar con Programador de tareas de Windows.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "sync": "npm run build && npm run start",
    "exe": "npm run build && pkg . --targets node18-win-x64 --output dist/ferromaderas-inventory-sync.exe"
  },
  "bin": "dist/index.js",
  "pkg": {
    "assets": [],
    "outputPath": "dist",
    "targets": ["node18-win-x64"]
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "pkg": "^5.8.1",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
